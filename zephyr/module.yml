name: ardep
build:
  kconfig: Kconfig
  cmake: .
  settings:
    board_root: .
    dts_root: .
    module_ext_root: .

runners:
  - file: scripts/runner/ardep.py
  - file: scripts/runner/ardep-uds.py

package-managers:
  pip:
    requirement-files:
      - scripts/requirements.txt
