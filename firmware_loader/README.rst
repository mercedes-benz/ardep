.. _firmware_loader:

Firmware Loader
###############

This firmware is used to update the firmware on ARDEP devices.
The bootloader runs this firmware under certain conditions, such as when a specific GPIO pin is held low during startup.
